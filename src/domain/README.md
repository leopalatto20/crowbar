# Domain

Domain types, invariants, and business rules belong here. Keep tests colocated with the behavior they verify.

Domain modules may depend on other domain modules and platform-neutral packages. They must not import React, React Native, Expo, database, data, feature, or UI modules.
