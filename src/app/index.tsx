import { FirstRunPreferenceScreen } from "../features/training-preferences/ui/first-run-preference-screen";

export default function Index(): React.JSX.Element {
  return <FirstRunPreferenceScreen />;
}
