# Data

Database setup, persistence schemas, and repositories belong here.

Data modules may depend on domain modules and persistence packages. They must not import app routes, features, or UI modules. Expose repository operations instead of SQLite queries to callers.
